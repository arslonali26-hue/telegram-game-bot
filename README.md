# Telegram Game Bot

Render uchun tayyor Telegram bot.

## Ishga tushirish

1. Render'da `New > Blueprint` yoki `New > Web Service` tanlang.
2. Ushbu repositoryni ulang.
3. Environment Variable qo'shing:

`BOT_TOKEN` = 8942775144:AAFsGOXxmLRPIf-FFfWewwTZ8lqfdT_XcKA

4. Build command:

```bash
pip install -r requirements.txt
```

5. Start command:

```bash
python bot.py
```

Health URL:

```text
https://SIZNING-SERVICE-NOMINGIZ.onrender.com/health
```

Tokenni GitHub kodiga yozmang. `users.json` va `clans.json` Render Free qayta ishga tushganda yo'qolishi mumkin; doimiy ma'lumotlar uchun keyin PostgreSQL/Supabase ulash kerak.
